# Railway managed Postgres variables are often injected as DATABASE_URL.
# Keep sslmode=require for hosted Postgres.
PORT=4000
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require"
FRONTEND_ORIGIN="*"
